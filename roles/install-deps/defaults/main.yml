# Known requirements per distro, please expand if you encounter breakage
known_distros:
  - CentOS
  - Fedora
  - Ubuntu
  - Debian
  - Red Hat Enterprise Linux
  - Arch Linux

debian_deps:
  - git
  - iptables-persistent
  - libssl

centos_deps:
  - iptables-services
  - git
  - openssl

ubuntu_deps:
  - git
  - iptables-persistent
  - libssl

fedora_deps:
  - iptables-services
  - git
  - openssl

arch_deps:
  - git
  - openssl
